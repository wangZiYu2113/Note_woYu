
### 6.1 有限体积法（径向）
空间取 N=5120 对径向面积进行离散
控制体边界取相邻节点中点，极每一个 N 点上下各取一半，在二维体现为面积，这就是所谓的控制体。由于模型所列公式依据的是单位体积下（单位面积下）热量守恒，因此需要根据面积为每一个 N 赋权，即
$$
w_i=x_{i+1/2}^{2}-x_{i-1/2}^{2},\qquad \sum_iw_i=1.
\tag{16}
$$
半离散：先对空间离散 将面积转换为通量。单位 N 所在的面积转换为两个内外两个界面**热量通量**的差 即 F(n-0.5 i) - F(n+0.5 i) ，由此，原方程即转化为
$$
\frac{\mathrm d\Theta_i}{\mathrm dt}
=\frac{F^T_{i-1/2}-F^T_{i+1/2}}{R^2w_i\rho_i c_{p,i}},
\qquad
\frac{\mathrm dU_i}{\mathrm dt}
=\frac{F^C_{i-1/2}-F^C_{i+1/2}}{R^2w_i}.
\tag{18}
$$
可见，消去了对空间的求导运算

